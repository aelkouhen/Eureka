spring:
    application:
        name: eureka

server:
    port: 8000

eureka:
    client:
        register-with-eureka: false
        fetch-registry: false
        serviceUrl:
            defaultZone: http://localhost:8000/eureka

logging:
    level:
        com:
            netflix:
                eureka: OFF
                discovery: OFF
